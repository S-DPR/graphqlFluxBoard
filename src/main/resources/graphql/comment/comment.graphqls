type Comment {
    id: ID!
    postId: ID!
    user: User
    comment: String
    replies: [Reply!]
    createdAt: String
}

input CommentInput {
    postId: ID!
    userId: ID!
    password: String
    comment: String
}
