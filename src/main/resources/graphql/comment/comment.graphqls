type Comment {
    id: ID!
    postId: ID!
    authorName: String
    comment: String
    replies: [Reply!]
    createdAt: String
}

input CommentInput {
    postId: ID!
    authorName: String
    password: String
    comment: String
}
