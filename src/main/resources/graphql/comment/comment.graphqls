type Comment {
    id: ID!
    postId: ID!
    user: User
    comment: String
    replies: [Reply!]
    createdAt: String
}

input SaveCommentInput {
    postId: ID!
    userId: ID!
    password: String!
    comment: String!
}
